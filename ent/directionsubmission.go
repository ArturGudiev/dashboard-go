// Code generated by ent, DO NOT EDIT.

package ent

import (
	"arturgudiev/dashboard/ent/direction"
	"arturgudiev/dashboard/ent/directionsubmission"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DirectionSubmission is the model entity for the DirectionSubmission schema.
type DirectionSubmission struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DirectionID holds the value of the "direction_id" field.
	DirectionID int `json:"direction_id,omitempty"`
	// ExecutionDate holds the value of the "execution_date" field.
	ExecutionDate time.Time `json:"execution_date,omitempty"`
	// Text holds the value of the "text" field.
	Text *string `json:"text"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DirectionSubmissionQuery when eager-loading is set.
	Edges        DirectionSubmissionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DirectionSubmissionEdges holds the relations/edges for other nodes in the graph.
type DirectionSubmissionEdges struct {
	// Direction holds the value of the direction edge.
	Direction *Direction `json:"direction,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DirectionOrErr returns the Direction value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DirectionSubmissionEdges) DirectionOrErr() (*Direction, error) {
	if e.Direction != nil {
		return e.Direction, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: direction.Label}
	}
	return nil, &NotLoadedError{edge: "direction"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DirectionSubmission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case directionsubmission.FieldID, directionsubmission.FieldDirectionID:
			values[i] = new(sql.NullInt64)
		case directionsubmission.FieldText:
			values[i] = new(sql.NullString)
		case directionsubmission.FieldExecutionDate:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DirectionSubmission fields.
func (_m *DirectionSubmission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case directionsubmission.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case directionsubmission.FieldDirectionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field direction_id", values[i])
			} else if value.Valid {
				_m.DirectionID = int(value.Int64)
			}
		case directionsubmission.FieldExecutionDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field execution_date", values[i])
			} else if value.Valid {
				_m.ExecutionDate = value.Time
			}
		case directionsubmission.FieldText:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field text", values[i])
			} else if value.Valid {
				_m.Text = new(string)
				*_m.Text = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DirectionSubmission.
// This includes values selected through modifiers, order, etc.
func (_m *DirectionSubmission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDirection queries the "direction" edge of the DirectionSubmission entity.
func (_m *DirectionSubmission) QueryDirection() *DirectionQuery {
	return NewDirectionSubmissionClient(_m.config).QueryDirection(_m)
}

// Update returns a builder for updating this DirectionSubmission.
// Note that you need to call DirectionSubmission.Unwrap() before calling this method if this DirectionSubmission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DirectionSubmission) Update() *DirectionSubmissionUpdateOne {
	return NewDirectionSubmissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DirectionSubmission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DirectionSubmission) Unwrap() *DirectionSubmission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DirectionSubmission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DirectionSubmission) String() string {
	var builder strings.Builder
	builder.WriteString("DirectionSubmission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("direction_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DirectionID))
	builder.WriteString(", ")
	builder.WriteString("execution_date=")
	builder.WriteString(_m.ExecutionDate.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.Text; v != nil {
		builder.WriteString("text=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// DirectionSubmissions is a parsable slice of DirectionSubmission.
type DirectionSubmissions []*DirectionSubmission
