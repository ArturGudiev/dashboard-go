// Code generated by ent, DO NOT EDIT.

package ent

import (
	"arturgudiev/dashboard/ent/direction"
	"arturgudiev/dashboard/ent/directionsubmission"
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DirectionSubmissionCreate is the builder for creating a DirectionSubmission entity.
type DirectionSubmissionCreate struct {
	config
	mutation *DirectionSubmissionMutation
	hooks    []Hook
}

// SetDirectionID sets the "direction_id" field.
func (_c *DirectionSubmissionCreate) SetDirectionID(v int) *DirectionSubmissionCreate {
	_c.mutation.SetDirectionID(v)
	return _c
}

// SetExecutionDate sets the "execution_date" field.
func (_c *DirectionSubmissionCreate) SetExecutionDate(v time.Time) *DirectionSubmissionCreate {
	_c.mutation.SetExecutionDate(v)
	return _c
}

// SetText sets the "text" field.
func (_c *DirectionSubmissionCreate) SetText(v string) *DirectionSubmissionCreate {
	_c.mutation.SetText(v)
	return _c
}

// SetNillableText sets the "text" field if the given value is not nil.
func (_c *DirectionSubmissionCreate) SetNillableText(v *string) *DirectionSubmissionCreate {
	if v != nil {
		_c.SetText(*v)
	}
	return _c
}

// SetID sets the "id" field.
func (_c *DirectionSubmissionCreate) SetID(v int) *DirectionSubmissionCreate {
	_c.mutation.SetID(v)
	return _c
}

// SetDirection sets the "direction" edge to the Direction entity.
func (_c *DirectionSubmissionCreate) SetDirection(v *Direction) *DirectionSubmissionCreate {
	return _c.SetDirectionID(v.ID)
}

// Mutation returns the DirectionSubmissionMutation object of the builder.
func (_c *DirectionSubmissionCreate) Mutation() *DirectionSubmissionMutation {
	return _c.mutation
}

// Save creates the DirectionSubmission in the database.
func (_c *DirectionSubmissionCreate) Save(ctx context.Context) (*DirectionSubmission, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *DirectionSubmissionCreate) SaveX(ctx context.Context) *DirectionSubmission {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DirectionSubmissionCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DirectionSubmissionCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *DirectionSubmissionCreate) check() error {
	if _, ok := _c.mutation.DirectionID(); !ok {
		return &ValidationError{Name: "direction_id", err: errors.New(`ent: missing required field "DirectionSubmission.direction_id"`)}
	}
	if v, ok := _c.mutation.DirectionID(); ok {
		if err := directionsubmission.DirectionIDValidator(v); err != nil {
			return &ValidationError{Name: "direction_id", err: fmt.Errorf(`ent: validator failed for field "DirectionSubmission.direction_id": %w`, err)}
		}
	}
	if _, ok := _c.mutation.ExecutionDate(); !ok {
		return &ValidationError{Name: "execution_date", err: errors.New(`ent: missing required field "DirectionSubmission.execution_date"`)}
	}
	if v, ok := _c.mutation.ID(); ok {
		if err := directionsubmission.IDValidator(v); err != nil {
			return &ValidationError{Name: "id", err: fmt.Errorf(`ent: validator failed for field "DirectionSubmission.id": %w`, err)}
		}
	}
	if len(_c.mutation.DirectionIDs()) == 0 {
		return &ValidationError{Name: "direction", err: errors.New(`ent: missing required edge "DirectionSubmission.direction"`)}
	}
	return nil
}

func (_c *DirectionSubmissionCreate) sqlSave(ctx context.Context) (*DirectionSubmission, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *DirectionSubmissionCreate) createSpec() (*DirectionSubmission, *sqlgraph.CreateSpec) {
	var (
		_node = &DirectionSubmission{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(directionsubmission.Table, sqlgraph.NewFieldSpec(directionsubmission.FieldID, field.TypeInt))
	)
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := _c.mutation.ExecutionDate(); ok {
		_spec.SetField(directionsubmission.FieldExecutionDate, field.TypeTime, value)
		_node.ExecutionDate = value
	}
	if value, ok := _c.mutation.Text(); ok {
		_spec.SetField(directionsubmission.FieldText, field.TypeString, value)
		_node.Text = &value
	}
	if nodes := _c.mutation.DirectionIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   directionsubmission.DirectionTable,
			Columns: []string{directionsubmission.DirectionColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(direction.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.DirectionID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DirectionSubmissionCreateBulk is the builder for creating many DirectionSubmission entities in bulk.
type DirectionSubmissionCreateBulk struct {
	config
	err      error
	builders []*DirectionSubmissionCreate
}

// Save creates the DirectionSubmission entities in the database.
func (_c *DirectionSubmissionCreateBulk) Save(ctx context.Context) ([]*DirectionSubmission, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*DirectionSubmission, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DirectionSubmissionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *DirectionSubmissionCreateBulk) SaveX(ctx context.Context) []*DirectionSubmission {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *DirectionSubmissionCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *DirectionSubmissionCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
