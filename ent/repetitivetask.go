// Code generated by ent, DO NOT EDIT.

package ent

import (
	"arturgudiev/dashboard/ent/repetitivetask"
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RepetitiveTask is the model entity for the RepetitiveTask schema.
type RepetitiveTask struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Tags holds the value of the "tags" field.
	Tags []string `json:"tags,omitempty"`
	// Closed holds the value of the "closed" field.
	Closed bool `json:"closed,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// OnceInDays holds the value of the "once_in_days" field.
	OnceInDays *int `json:"once_in_days,omitempty"`
	// OnceInWeeks holds the value of the "once_in_weeks" field.
	OnceInWeeks *int `json:"once_in_weeks,omitempty"`
	// OnceInMonths holds the value of the "once_in_months" field.
	OnceInMonths *int `json:"once_in_months,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RepetitiveTaskQuery when eager-loading is set.
	Edges        RepetitiveTaskEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RepetitiveTaskEdges holds the relations/edges for other nodes in the graph.
type RepetitiveTaskEdges struct {
	// Executions holds the value of the executions edge.
	Executions []*RepetitiveTaskExecution `json:"executions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ExecutionsOrErr returns the Executions value or an error if the edge
// was not loaded in eager-loading.
func (e RepetitiveTaskEdges) ExecutionsOrErr() ([]*RepetitiveTaskExecution, error) {
	if e.loadedTypes[0] {
		return e.Executions, nil
	}
	return nil, &NotLoadedError{edge: "executions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RepetitiveTask) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case repetitivetask.FieldTags:
			values[i] = new([]byte)
		case repetitivetask.FieldClosed:
			values[i] = new(sql.NullBool)
		case repetitivetask.FieldID, repetitivetask.FieldOnceInDays, repetitivetask.FieldOnceInWeeks, repetitivetask.FieldOnceInMonths:
			values[i] = new(sql.NullInt64)
		case repetitivetask.FieldDescription, repetitivetask.FieldNotes:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RepetitiveTask fields.
func (_m *RepetitiveTask) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case repetitivetask.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case repetitivetask.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case repetitivetask.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case repetitivetask.FieldClosed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field closed", values[i])
			} else if value.Valid {
				_m.Closed = value.Bool
			}
		case repetitivetask.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case repetitivetask.FieldOnceInDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field once_in_days", values[i])
			} else if value.Valid {
				_m.OnceInDays = new(int)
				*_m.OnceInDays = int(value.Int64)
			}
		case repetitivetask.FieldOnceInWeeks:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field once_in_weeks", values[i])
			} else if value.Valid {
				_m.OnceInWeeks = new(int)
				*_m.OnceInWeeks = int(value.Int64)
			}
		case repetitivetask.FieldOnceInMonths:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field once_in_months", values[i])
			} else if value.Valid {
				_m.OnceInMonths = new(int)
				*_m.OnceInMonths = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RepetitiveTask.
// This includes values selected through modifiers, order, etc.
func (_m *RepetitiveTask) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryExecutions queries the "executions" edge of the RepetitiveTask entity.
func (_m *RepetitiveTask) QueryExecutions() *RepetitiveTaskExecutionQuery {
	return NewRepetitiveTaskClient(_m.config).QueryExecutions(_m)
}

// Update returns a builder for updating this RepetitiveTask.
// Note that you need to call RepetitiveTask.Unwrap() before calling this method if this RepetitiveTask
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RepetitiveTask) Update() *RepetitiveTaskUpdateOne {
	return NewRepetitiveTaskClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RepetitiveTask entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RepetitiveTask) Unwrap() *RepetitiveTask {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RepetitiveTask is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RepetitiveTask) String() string {
	var builder strings.Builder
	builder.WriteString("RepetitiveTask(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", _m.Tags))
	builder.WriteString(", ")
	builder.WriteString("closed=")
	builder.WriteString(fmt.Sprintf("%v", _m.Closed))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	if v := _m.OnceInDays; v != nil {
		builder.WriteString("once_in_days=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.OnceInWeeks; v != nil {
		builder.WriteString("once_in_weeks=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.OnceInMonths; v != nil {
		builder.WriteString("once_in_months=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteByte(')')
	return builder.String()
}

// RepetitiveTasks is a parsable slice of RepetitiveTask.
type RepetitiveTasks []*RepetitiveTask
